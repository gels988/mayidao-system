# 手动验证清单 (MANUAL_VERIFICATION_CHECKLIST.md)

本清单用于在自动化部署和测试完成后，进行人工确认的关键步骤。

## 1. 敏感模块访问限制验证

- [ ] **验证管理看板 (数据库资料.html)**
    - [ ] 在 **本地环境** 打开 `d:\预测手机端总装11\index_old_backup.html` 或 `d:\预测手机端总装11\frontend\dashboard.html` (如果有)，确认可以正常访问和操作。
    - [ ] 访问 **云端部署 URL** (例如: `https://mayidao-gels988.vercel.app/数据库资料.html`)。
    - [ ] **预期结果**: 应该显示 404 Not Found 或被重定向到主页，**绝对不能** 显示管理界面。

- [ ] **验证自学模块 (self_learn.html)**
    - [ ] 在 **本地环境** 打开 `d:\预测手机端总装11\frontend\self_learn.html`，确认可以正常访问。
    - [ ] 访问 **云端部署 URL** (例如: `https://mayidao-gels988.vercel.app/self_learn.html`)。
    - [ ] **预期结果**: 应该显示 404 Not Found 或被重定向到主页。

## 2. 移动端适配测试

- [ ] **手机浏览器访问**
    - [ ] 使用真实手机 (iOS/Android) 访问部署链接。
    - [ ] 检查页面布局是否错乱。
    - [ ] 检查字体大小是否合适。
    - [ ] 检查交互元素 (按钮、输入框) 是否易于点击。

- [ ] **功能测试**
    - [ ] 在手机端尝试进行一次完整的预测流程。
    - [ ] 确认数据加载速度在可接受范围内。

## 3. API 跨域与连接测试

- [ ] **API 直接访问**
    - [ ] 访问 `https://mayidao-gels988.vercel.app/api/health`。
    - [ ] **预期结果**: 返回 JSON 格式的健康状态信息 (status: ok)。

## 4. 极端情况测试

- [ ] **断网测试**
    - [ ] 在加载页面后断开网络，检查是否有友好的离线提示 (如果配置了 PWA)。
    - [ ] 恢复网络后，检查是否自动重连。

## 5. 提交确认

- [ ] 确认所有本地修改都已包含在 Git 提交中。
- [ ] 确认 `DEPLOY_INFO.txt` 中的信息准确无误。

---
**验证完成人**: ____________________
**验证日期**: ____________________
