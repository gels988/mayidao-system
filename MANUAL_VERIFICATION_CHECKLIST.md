# 📋 蚂蚁岛系统 - 手动验证清单 (MANUAL_VERIFICATION_CHECKLIST.md)

本清单包含无法通过脚本自动验证的项目，请在部署完成后逐项确认。

## 🔐 安全与权限验证 (关键)

- [ ] **敏感页面外部访问测试**
  - [ ] 在 **手机浏览器** 或 **非本机** 浏览器中访问 `https://your-app.vercel.app/数据库资料.html`。
  - [ ] 预期结果：显示 **404 Not Found** 或 **403 Forbidden**。
  - [ ] 在 **手机浏览器** 或 **非本机** 浏览器中访问 `https://your-app.vercel.app/self_learn.html`。
  - [ ] 预期结果：显示 **404 Not Found** 或 **403 Forbidden**。

- [ ] **本地文件访问**
  - [ ] 在 **本机 (D盘所在电脑)** 双击打开 `d:\预测手机端总装11\数据库资料.html`。
  - [ ] 预期结果：页面正常加载，图表显示数据（需确保 API 服务正常）。

## 📱 移动端适配验证

- [ ] **主页显示**
  - [ ] 手机访问首页 `https://your-app.vercel.app/`。
  - [ ] 预期结果：布局自适应，无横向滚动条。
  
- [ ] **图表交互**
  - [ ] 在手机上点击图表数据点。
  - [ ] 预期结果：Tooltip 提示框正常显示，无遮挡。

## 💾 数据完整性验证

- [ ] **数据更新**
  - [ ] 在本地打开 `数据库资料.html`，添加一条测试数据。
  - [ ] 刷新 `https://your-app.vercel.app/` (如果首页有引用数据)。
  - [ ] 检查 Supabase 后台，确认数据已写入。

## 🌐 网络环境验证

- [ ] **跨域请求 (CORS)**
  - [ ] 打开浏览器开发者工具 (F12) -> Network。
  - [ ] 刷新页面，查看 `/api/stats` 或 `/api/health` 请求。
  - [ ] 预期结果：Status 200，无 CORS 红色报错。

## 🧹 维护与清理

- [ ] **部署日志**
  - [ ] 检查 `DEPLOY_INFO.txt` 文件内容是否准确。
  - [ ] 确认没有将 `.env` 文件意外提交到 GitHub (检查 GitHub 仓库)。

---
*最后更新时间: 2026-02-09*
